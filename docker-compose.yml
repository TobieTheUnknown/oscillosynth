version: '3.8'

services:
  oscillosynth:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
